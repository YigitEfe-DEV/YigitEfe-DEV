# DevToolKit

DevToolKit is a responsive developer dashboard for quick access to common tools, with search, favorites, recents, keyboard navigation, and local persistence.

## Features

- Global search
- Tool categories
- Favorites system
- Recent tools
- Command palette with `Ctrl+K`
- JSON formatter and validator
- JWT decoder
- UUID generator
- Timestamp converter
- SHA-256 hash generator
- Regex tester
- Color converter for HEX, RGB, and HSL
- Text diff checker
- Dark and light mode
- LocalStorage persistence
- Smooth responsive layout

## Screenshots

Add screenshots here after capturing the dashboard in light and dark mode.

## Installation

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Usage

- Use the search field to filter tools.
- Click categories to narrow the catalog.
- Press `Ctrl+K` to open the command palette.
- Favorite frequently used tools for faster access.
- Switch theme from the sidebar.

## Roadmap

- Add per-tool history and saved presets
- Improve diff visualization with inline highlights
- Add clipboard actions for each utility
- Expand the tool catalog with encoding and formatting helpers
